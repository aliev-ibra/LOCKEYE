// ... existing code ...

if (typeof window !== 'undefined') {
  // Access localStorage here
  const lastRotation = localStorage.getItem('your_key');
  // Perform operations with lastRotation
}

// ... existing code ...